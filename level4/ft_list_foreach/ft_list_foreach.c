/*Nombre de la tarea: ft_list_foreach
Archivos esperados: ft_list_foreach.c, ft_list.h
Funciones permitidas:

-------------------------------------------------------------------------

Escriba una función que reciba una lista y un puntero a función, y aplique
esta función a cada elemento de la lista.

Debe declararse de la siguiente manera:

void ft_list_foreach(t_list *begin_list, void (*f)(void *));

La función a la que apunta f se utilizará de la siguiente manera:

(*f)(list_ptr->data);

Debe usar la siguiente estructura y entregarla como un archivo llamado
ft_list.h:*/

#include "ft_list.h"

void ft_list_foreach(t_list *begin_list, void (*f)(void *))
{

}
